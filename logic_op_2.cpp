#include "logic_op_2.h"
#include "math.h"

returns calc(logic base){
    ifstream file (base.file_name);
    returns res;
    int i, j;
    string full, piece;
    getline(file, full);
    while (getline(file, full)){

    }
    return res;
}

